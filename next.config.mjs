/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: false,
  reactCompiler: true,
  images: {
    domains: ["images.pexels.com"],
  },
};

export default nextConfig;
